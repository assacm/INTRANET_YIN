
@{
    ViewData["Title"] = "Factura #0123";
}

<partial name="_Menu" />

<section id="encabezado" class="pt-5">
	<div class="container pt-5">
		<div class="row">
			<div class="col-lg-6 col-md-6 col-12">
				<h1>Factura</h1>
				<h2>ORDEN DE COMPRA #000123</h2>
			</div>
			<div class="col-lg-6 col-md-6 col-12">
				<div class="row">
					<div class="col-lg-6 col-md-6 col-12"></div>
					<div class="col-lg-6 col-md-6 col-12">
						<a href="home.html" class="btn-regresar btn-y"> &lt; Regresar</a>
					</div>
				</div>
			</div>
			<div class="col-lg-12 col-12 pt-4">
				<hr>
			</div>
		</div>
	</div>
</section>

<section class="container__factura">
    <div class="row row-eq-height">
        <div class="col-lg-5 col-md-5">
            <div class="row">
                <div class="col-lg-4 col-md-4">
                        <p>RFC emisor:</p> 
                        <p>Nombre emisor:</p>
                        <p>Folio:</p>
                        <p>RFC receptor:</p>
                        <p>Código postal del receptor:</p>
                        <p>Régimen fiscal receptor:</p>
                        <p>Uso CFDI:</p>
                </div>
                <div class="col-lg-8 col-md-8">
                    <p>RFC emisor:</p>
                    <p>Nombre emisor:</p>
                    <p>Folio:</p>
                    <p>RFC receptor:</p>
                    <p>Código postal del receptor:</p>
                    <p>Régimen fiscal receptor:</p>
                    <p>Uso CFDI:</p>
                </div>
            </div>
        </div>
        <div class="col-2 line-yellow"></div>
        <div class="col-lg-5 col-md-5">
            <div class="row">
                <div class="col-lg-4 col-md-4">
                    <p>Folio fiscal:</p>
                    <p>No. de serie del CSD:</p>
                    <p>Serie:</p>
                    <p>Código postal, fecha y hora de emisión:</p>
                    <p>Efecto de comprobante:</p>
                    <p>Régimen fiscal:</p>
                    <p>Exportación:</p>
                </div>
                <div class="col-lg-8 col-md-8">
                    <p>Folio fiscal:</p>
                    <p>No. de serie del CSD:</p>
                    <p>Serie:</p>
                    <p>Código postal, fecha y hora de emisión:</p>
                    <p>Efecto de comprobante:</p>
                    <p>Régimen fiscal:</p>
                    <p>Exportación:</p>
                </div>
            </div>
        </div>
    </div>
  
</section>

<section id="tabla" class="p-3">
    <div id="tabla" class="py-5 px-5">
        <table class="table table-striped table-responsive">
            <thead>
                @*<tr class="table-title">
                    <th colspan="8">TABLA DE REPORTE</th>
                </tr>*@
                <tr>
                    <th scope="col">Id Producto</th>
                    <th scope="col">Descripción</th>
                    <th scope="col">Precio</th>
                    <th scope="col">Importe</th>
                    <th scope="col">Descuento</th>
                    <th scope="col">Sub Total</th>
                   @* <th scope="col">Moneda</th>
                    <th scope="col">Ver Factura</th>*@
                </tr>
            </thead>
            <tbody>
                    <tr>
                        <td>NoDoc</td>
                        <td>consectetur adipiscing elit</td>
                        <td>Precio</td>
                        <td>Importe</td>
                        <td>Descuento</td>
                        <td>Subtotal</td>
                    </tr>
            </tbody>
  @*          @if (Model == null || Model.Facturas == null || Model.Facturas.Any() == false)
            {
                <tr>
                    <td colspan="8">Sin facturas</td>
                </tr>
            }*@
           
        </table>
    </div>
</section>

@section Scripts{
    <script src="~/js/menu.js" asp-append-version="true"></script>
    <script type="module">

        import { customMenu } from '/js/menu-options.js'
        let urlMenu = '@Url.Action("MenuOptions","Usuarios")';
        customMenu(urlMenu);

    </script>
}
@*
    <section id="tabla">
        <div class="container mb-5">
            <div class="row">
                <div class="col-lg-12">
                    <div class="text-right">
                        <button class="btn btn-y"><i class="fa fa-download"></i> Descargar</button>
                    </div>*@

                @*
                    <div id="tabla" class="pt-4">
                        <table class="table table-striped table-responsive">
                            <thead>
                                <tr class="table-title">
                                    <th colspan="8">TABLA DE REPORTE</th>
                                </tr>
                                <tr>
                                    <th scope="col">Id Factura</th>
                                    <th scope="col">Razón social</th>
                                    <th scope="col">Nombre del cliente</th>
                                    <th scope="col">Estado Factura</th>
                                    <th scope="col">Fecha de facturación</th>
                                    <th scope="col">Total Factura</th>
                                    <th scope="col">Moneda</th>
                                    <th scope="col">Ver Factura</th>
                                </tr>
                            </thead>
                            @if (Model == null || Model.Facturas == null || Model.Facturas.Any() == false)
                            {
                                <tr>
                                    <td colspan="8">Sin facturas</td>
                                </tr>
                            }
                            else
                            {
                                <tbody>

                                    @foreach (Documento factura in Model.Facturas)
                                    {
                                        <tr>
                                            <th scope="row">1 @factura.NoDocumento</th>
                                            <td>consectetur adipiscing elit</td>
                                            <td>@factura.NombreS</td>
                                            <td>@Enum.GetName(typeof(EstadosDoc),Model.Estado)</td>
                                            <td>@factura.Fecha</td>
                                            <td>@factura.Total</td>
                                            <td>@factura.Moneda</td>
                                            <td><a href="">Ver Factura</a></td>
                                        </tr>
                                    }
                                </tbody>
                                <tfoot>
                                    <tr>
                                        <td colspan="5">Total</td>
                                        <td>@Model.Total</td>
                                        <td>MXN</td>
                                    </tr>
                                </tfoot>
                            }
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>


    <div class="row">
        <div class="col-12"><hr /></div>
    </div>
    <div class="row row-eq-height">
        <div class="col-lg-6 col-md-6">
            <div class="row">
                <div class="col-lg-4 col-md-4">
                    <p>RFC emisor:</p>
                    <p>Nombre emisor:</p>
                    <p>Folio:</p>
                    <p>RFC receptor:</p>
                    <p>Código postal del receptor:</p>
                    <p>Régimen fiscal receptor:</p>
                    <p>Uso CFDI:</p>
                </div>
                <div class="col-lg-8 col-md-8">
                    <p>RFC emisor:</p>
                    <p>Nombre emisor:</p>
                    <p>Folio:</p>
                    <p>RFC receptor:</p>
                    <p>Código postal del receptor:</p>
                    <p>Régimen fiscal receptor:</p>
                    <p>Uso CFDI:</p>
                </div>
            </div>
        </div>
        <div class="col-lg-6 col-md-6">
            <div class="row">
                <div class="col-lg-4 col-md-4">
                    <p>Folio fiscal:</p>
                    <p>No. de serie del CSD:</p>
                    <p>Serie:</p>
                    <p>Código postal, fecha y hora de emisión:</p>
                    <p>Efecto de comprobante:</p>
                    <p>Régimen fiscal:</p>
                    <p>Exportación:</p>
                </div>
                <div class="col-lg-8 col-md-8">
                    <p>Folio fiscal:</p>
                    <p>No. de serie del CSD:</p>
                    <p>Serie:</p>
                    <p>Código postal, fecha y hora de emisión:</p>
                    <p>Efecto de comprobante:</p>
                    <p>Régimen fiscal:</p>
                    <p>Exportación:</p>
                </div>
            </div>
        </div>
    </div>

</section>*@